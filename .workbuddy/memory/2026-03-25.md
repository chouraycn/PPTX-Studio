# 2026-03-25

## SKILL.md 分析 + 改进执行

对 PPTX Studio 的 `SKILL.md` 进行了完整分析，并执行了四项改进：

- **动画丢失警告**：在 Decision Flow 决策树和 Mode 1（Template Apply）Step 0 加入了动画检测代码片段 + 用户确认提示，防止用户无感知地丢失动画
- **`--no-restructure` 使用时机说明**：在 `beautify_ppt.py` 脚本说明区加入对比简表（4 种场景判断）
- **Node.js 环境检测**：在 Creating from Scratch 区加入 `node --version` / `npm --version` 检查步骤，以及 Homebrew 安装命令和无 Node.js 时的备用方案
- **`--language auto` 规则透明化**：在 Speaker Notes 常用选项区补充检测原理（Unicode 汉字字符占比 >10% 判中文），并说明中英混排的处理行为
